---
import { Image } from 'astro:assets';
import shotLive from '../assets/screens/shot-live.png';
import shotAbschluss from '../assets/screens/shot-abschluss.png';
import history from '../assets/screens/history.png';
import shotDetail from '../assets/screens/shot-detail.png';

const shots = [
  {
    src: shotLive,
    alt: 'Aufnahme-Screen mit Zeit ab Pumpenstart, Live-Gewicht und Fortschritt zum Ziel-Yield',
    caption: 'Aufnahme: Zeit ab Pumpenstart, Gewicht, Ziel-Fortschritt',
  },
  {
    src: shotAbschluss,
    alt: 'Abschluss-Screen mit den Kennzahlen des Shots und dem Delta zum Vorgänger',
    caption: 'Abschluss: Kennzahlen und Delta zum letzten Shot',
  },
  {
    src: history,
    alt: 'History-Liste, nach Bohne gruppiert, mit Filter',
    caption: 'History: nach Bohne, filterbar',
  },
  {
    src: shotDetail,
    alt: 'Shot-Detail mit Gewichtskurve und Flow-Kurve, First Drip und Pumpenstopp markiert',
    caption: 'Detail: Gewichts- und Flow-Kurve, First Drip, Pumpenstopp',
  },
];
---

<section id="app" class="py-24 px-6">
  <div class="max-w-6xl mx-auto">
    <div class="max-w-2xl mb-12">
      <p class="label mb-4 animate">Ein Blick hinein</p>
      <h2 class="font-display text-3xl sm:text-4xl lg:text-5xl leading-tight text-balance mb-5 animate">
        Zahlen sind der Inhalt.
      </h2>
      <p class="text-ink-2 leading-relaxed animate">
        Dunkel, warm und ruhig, damit um sechs Uhr morgens die Ziffern
        auffallen und nicht das Interface.
      </p>
    </div>

    <!-- Mobil als Snap-Reihe, ab Desktop als Raster -->
    <div class="flex md:grid md:grid-cols-4 gap-5 overflow-x-auto md:overflow-visible snap-x snap-mandatory pb-4 md:pb-0 -mx-6 px-6 md:mx-0 md:px-0">
      {shots.map((shot, i) => (
        <figure class={`animate stagger-${i + 1} shrink-0 w-[70vw] sm:w-[45vw] md:w-auto snap-center m-0`}>
          <div class="rounded-2xl border border-border overflow-hidden bg-bg-deep">
            <!-- Seitenverhaeltnis der Aufnahme (1206x2622): der Screen bleibt ganz. -->
            <div class="relative aspect-[1206/2622] overflow-hidden">
              <Image
                src={shot.src}
                alt={shot.alt}
                width={640}
                format="webp"
                class="absolute inset-0 w-full h-full object-cover object-top"
              />
            </div>
          </div>
          <figcaption class="mt-3 text-sm text-ink-3 leading-relaxed">
            {shot.caption}
          </figcaption>
        </figure>
      ))}
    </div>
  </div>
</section>
